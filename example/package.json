{
  "name": "node-console-progress-bar-tqdm-example",
  "version": "1.0.0",
  "scripts": {
    "preinstall": "cd .. && npm ci && npm run build",
    "start": "node index.mjs",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "colors": "1.4.0",
    "inquirer": "9.2.14",
    "node-console-progress-bar-tqdm": "file:.."
  },
  "devDependencies": {
    "@types/node": "^20.11.17",
    "typescript": "^5.3.3"
  }
}
